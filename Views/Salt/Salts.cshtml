@model IEnumerable<Salt>

@{
    ViewData["Title"] = "Salt info";
}

<section>
    <h3>List of Salts</h3>
    <ul>
        <section>
            @foreach (var salt in Model)
            {
                <li>@salt.Name</li>
                <li>@salt.PartitionKey</li>
            }
        </section>
        HELLLOOOOOO!
    </ul>
</section>
